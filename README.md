# bulletfarm-test-repo-beta
Test repository for bulletfarm agent operator (beta)

## Features
- Added structured logging using the log/slog package.
- Implemented logging for HTTP requests including method, path, status code, and response duration.
- Introduced a health check endpoint at /healthz.
